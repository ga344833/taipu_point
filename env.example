# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=true
TIME_ZONE=Asia/Taipei

# Database Configuration
# 使用 PostgreSQL（Docker 環境）
DB_ENGINE=postgresql
DB_NAME=taipu_point
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=postgres
DB_PORT=5432

# 本地開發使用 SQLite（可選）
# DB_ENGINE=sqlite
# DB_NAME=db.sqlite3

# CORS
CSRF_CHECK=false
